{
  "name": "upload-to-qiniu-action",
  "version": "1.0.2",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "qiniu": "^7.14.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
